» implement .html on u.Request objects
	parse html markups coming from requests and build an utm object with it
	make it ready to be used like any utm object

» implement a cookie helper
	make cookie handling easier to the user
	bring some processing to client side to avoid server's session overcaching

» rename u.Animation to u.Fx
	make the class name smaller but understandable, simply

» xpath support
	enable xpath elements selection
	another option for those who like xpath

» code refactoring
	code is already clean. but why not make it even cleaner?
	improve code's readability in cost of its low size
